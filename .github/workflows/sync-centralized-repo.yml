name: Sync with Centralized Repository Test

on:
  push:
    branches:
      - main
      - develop
      - '**'
  pull_request:
    types: [closed]
    branches:
      - main

jobs:
  sync-centralized-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout centralized repository
        uses: actions/checkout@v4
        with:
          repository: sabbannagari/api-automation-with-agentic
          token: ${{ secrets.CENTRALIZED_REPO_TOKEN }}
          path: automation
          fetch-depth: 0

      - name: Get changes from centralized repository
        run: |
          cd automation
          echo "=== Centralized Repository Information ==="
          echo "Repository: $(git remote get-url origin)"
          echo "Current branch: $(git branch --show-current)"
          echo ""
          echo "=== Latest commits in centralized repo ==="
          git log -5 --oneline --decorate
          echo ""
          echo "=== Recent changes (last 24 hours) ==="
          git log --since="24 hours ago" --pretty=format:"%h - %an, %ar : %s" || echo "No changes in last 24 hours"
          echo ""
          echo "=== Files changed in latest commit ==="
          git diff --name-status HEAD~1 HEAD || echo "No previous commit to compare"
          ls -larth

      - name: Create summary
        run: |
          echo "### Sync Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ Successfully checked out centralized repository" >> $GITHUB_STEP_SUMMARY

      - name: Upload centralized repo
        uses: actions/upload-artifact@v4
        with:
          name: centralized-repo
          path: automation

  create-testcases:
    needs: sync-centralized-repo
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.actor }}/agentic-automation:build5-efe72f9
      options: -v ${{ github.workspace }}/automation:/app/automation:rw
      env:
        ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
    steps:
      - name: Download centralized repo
        uses: actions/download-artifact@v4
        with:
          name: centralized-repo
          path: automation

      - name: Run TestCases
        run: |
           ls -alrth
           cd automation
           ls -larth 
           cd automation
           ls -alrth
          

